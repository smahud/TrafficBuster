/**
 * BACKEND - sessionTimeout.js
 * - Middleware placeholder agar tidak error saat di-require.
 * - Siap ditingkatkan untuk keep-alive/timeout sesuai To-Do (kini hanya next()).
 */

'use strict';

module.exports = function sessionTimeout(req, res, next) {
  return next();
};
